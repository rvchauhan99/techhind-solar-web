export const Z_INDEX_LAYERS = {
  tableStickyBody: 10,
  tableStickyFilter: 20,
  tableStickyHeader: 30,
  drawerBackdrop: 80,
  drawerPanel: 90,
  // Above MUI theme.zIndex.drawer (1200); align with theme.zIndex.modal (1300) so Radix Dialogs stack over MUI Drawer/Modal.
  modalBackdrop: 1290,
  modalContent: 1300,
};

